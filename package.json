{
  "name": "linkedin-scraper-app",
  "version": "1.0.0",
  "description": "LinkedIn job scraper with AI email generation",
  "main": "server.js",
  "scripts": {
    "dev": "nodemon server.js",
    "start": "node server.js",
    "build": "mkdir -p public && cp *.html public/ 2>/dev/null || true && cp *.js public/ 2>/dev/null || true && cp -r services public/ 2>/dev/null || true && echo 'Build completed successfully'",
    "test": "echo 'No tests specified'"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "body-parser": "^1.20.2",
    "multer": "^1.4.5-lts.1",
    "node-fetch": "^3.3.2",
    "sqlite3": "^5.1.7",
    "googleapis": "^126.0.1",
    "nodemailer": "^6.9.7",
    "dotenv": "^16.3.1",
    "uuid": "^9.0.1",
    "axios": "^1.6.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "keywords": ["linkedin", "scraper", "apify", "email", "automation"],
  "author": "Your Name",
  "license": "MIT"
}
